# Add these values in Streamlit Community Cloud → App settings → Secrets.
# Never commit the real secrets.toml file.
FINLENS_PASSWORD = "replace-with-a-long-private-password"
OPENAI_API_KEY = "managed-in-your-deployment-secrets"
OPENAI_MODEL = "gpt-4.1-mini"
